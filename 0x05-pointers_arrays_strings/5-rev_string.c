#include "main.h"
#include <stdio.h>

/**
 * rev_string - a function that reverses a string
 * @s: input string
 * Return: nothing
 */

void rev_string(char *s)
{
int x, y;
char tempo;

for (x = 0; s[x] != '\0'; ++x)
;

while(x--)
{
    tempo = s[x];
}
s* = tempo;
}
